# BM Face Round 1 — Compact checkpoint
Cập nhật: 2026-09-14. Phạm vi: riêng luồng BM Face / BM Identity Attendance và portal nhân viên `nv.redtigerhead.com`. Đánh dấu “chưa xác nhận” cho mọi điểm chưa có bằng chứng vận hành.

- **Goal:** Nền tảng danh tính và chấm công độc lập vendor cho một nhà máy Bình Minh. Round 1 chỉ tập trung chấm công, một DAH 1035 làm pilot; mở cửa, đăng nhập Windows/SSO, MCP/AI automation để giai đoạn khác. BMA dùng cho nhân viên; web admin responsive dùng cho vận hành.
- **Hồ sơ quyết định:** `BM_Identity_Attendance_Goal_Whitepaper_v0.2.md` đã phân loại 147 câu A01–K14; 18 câu Round 1 ban đầu được xử lý ở mức discovery. Tám quyết định D-01–D-08 vẫn cần Minh An duyệt; v0.3 chưa ký. Việc thử nghiệm kỹ thuật không đồng nghĩa đã đóng Goal hoặc cho phép production.
- **Thiết bị:** Face1 / DAH 1035, ID `1605063`, IP LAN `192.168.1.227`, web port `80`, MAC `5c:f2:86:8b:58:58`; ping, TCP và web-login đã từng PASS. Ảnh bo mạch nhận diện Rockchip RV1109. Thông số RAM, dung lượng khuôn mặt, liveness và độ bền catalogue chưa được nghiệm chứng. Không flash/reset khi chưa backup, recovery và rollback.
- **Ranh giới vận hành:** Giữ firmware gốc và kết nối LAN qua BM Device Bridge trên MINHCOMP trong pilot. DAHAHI chỉ chạy song song để đối chiếu; đích cuối không phụ thuộc cloud vendor. Website Internet không gọi `192.168.1.227`; terminal/Bridge/AI không ghi PostgreSQL trực tiếp; không mở cổng 5432 ra Internet.
- **Luồng dữ liệu:** `Terminal → BM Device Bridge (raw-first SQLite/outbox) → BMA Integration API → PostgreSQL 17 native / binhminh_data → BMA và admin web`. BMA API là bên duy nhất ghi attendance; sự kiện gốc và bảng công tách biệt, có sequence, idempotency, retry/backoff, quarantine và audit. `nv.redtigerhead.com` là UI, không phải endpoint ingest.
- **Chấm công:** Phải phân biệt `IN`/`OUT` rõ ràng, không tự suy từ GPS/Wi-Fi hoặc cấu hình hướng “Nhập” hiện tại. Mã người terminal phải map với employee code đã duyệt. AI chỉ đề xuất, không tự tính/quyết định lương. Chính sách ca, OT, chốt công, fallback face fail và retention còn chờ ký.
- **Bằng chứng Bridge:** Checkpoint 07-09 ghi test cục bộ `POST /Subscribe/verify`, `body.personId = TEST-001`, source IP `127.0.0.1`; đó không phải callback thật từ Face1. Bridge v0.2.2 được báo forwarding staging; snapshot UI 08-09 ghi v0.2.3. Các mốc version này không chứng minh đã có IN→OUT thực, đóng session và hiển thị Công/Cá Nhân E2E.
- **Portal:** Clone trang DAHAHI `/dsKhach` phục vụ thử giao diện, dùng dữ liệu minh họa, không sao chép dữ liệu nhân sự thật. [nv.redtigerhead.com](https://nv.redtigerhead.com) hiện active, private theo Site; custom domain/SSL được báo active. Lần kiểm tra 14-09: Site version **8** đã publish thành công, source `2afad358051ed2f114de456282f246463071ba69` (08-09); portal hiển thị Bridge v0.2.3 và chưa có lượt vào/ra thật.
- **GitHub:** Snapshot source portal trước đó nằm ở [`nguyenmian3012-code/redtigerhead`, nhánh `feature/nv-portal-v1`](https://github.com/nguyenmian3012-code/redtigerhead/tree/feature/nv-portal-v1), commit `b2c80319f31d6a79846659f682e9e578c993d513`. Nhánh này là mốc push 06-09; **không xác nhận đồng bộ** với Site version 8 mới hơn. Không mô tả nhánh là production source hiện tại.
- **Quyết định cần duyệt D-01–D-08:** D-01 tên/Goal/phạm vi; D-02 hai nút VÀO/RA; D-03 format mã nhân viên; D-04 ngày chốt và quyền reopen; D-05 fallback nhận diện; D-06 HR policy owner; D-07 domain topology; D-08 privacy, retention và acceptance metrics. Đề xuất trong whitepaper chưa phải quyết định của sponsor.
- **Bằng chứng kỹ thuật còn thiếu:** T-01…T-07 gồm export/restore, protocol LAN và auth, WAN-off/vendor endpoints, fingerprint phần cứng, liveness/spoof, contract staging và inventory hệ cũ. Cần callback vật lý source `192.168.1.227`, mapping person ID thật→employee code, một lượt IN, một lượt OUT, đối soát PostgreSQL và BMA; chưa có chứng cứ đủ để đánh dấu production PASS.
- **Bước tiếp theo và gate:** Bắt đầu duyệt **D-01**, rồi lần lượt D-02–D-08. Sau đó đóng evidence, ký Goal v0.3/event contract/acceptance; pilot nhóm nhỏ chạy song song sổ tay và đánh giá dữ liệu sinh trắc học trước enrollment thật. Chấm công production vẫn **NO-GO** cho đến khi E2E và privacy gate đạt. Lịch nhắc BM Face Round 1 tiếp tục cho đến khi Minh An xác nhận hoàn thiện.
