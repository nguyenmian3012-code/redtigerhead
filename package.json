{
  "name": "redtigerhead",
  "version": "0.3.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "test": "node --test tests/*.test.mjs && node scripts/check-price-display.mjs",
    "preview": "astro preview",
    "deploy": "npm run build && wrangler deploy",
    "deploy:preview": "npm run build && wrangler deploy --env preview",
    "sync:prices": "node scripts/sync-prices.mjs"
  },
  "dependencies": {
    "astro": "^7.2.9"
  },
  "devDependencies": {
    "wrangler": "^4.125.0"
  }
}
