# ChatGPT / Codex project context

Follow `AGENTS.md` as the primary working agreement.

This is a standalone company website for Bình Minh at `redtigerhead.com`; it must remain separate from ABMT core. Read `README.md`, `docs/PROJECT_DIRECTION.md`, and `docs/PROJECT_STATE.md` before substantial work.

Use a `chatgpt/<task>` branch when editing through ChatGPT/Codex. Do not work concurrently on a branch owned by OpenClaw. Run `npm run check` before handing work back.
